const INIT_STATE = {
  startLatitude: '',
  startLongtitude: '',
  endLatitude: '',
  endLongtitude: '',
}

export default (state = INIT_STATE, action) => {
  switch(action.type) {
    default: return state;
  }
}
